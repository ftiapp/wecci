import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    /*
      WebP อย่างเดียว ไม่เอา AVIF

      AVIF ไฟล์เล็กกว่าจริง แต่เว็บนี้รันเป็น self-host (next start ในคอนเทนเนอร์)
      ตัวย่อภาพจึงเข้ารหัสด้วย CPU ของคอนเทนเนอร์เอง และ AVIF ที่ความกว้างระดับ
      แบนเนอร์เต็มจอใช้เวลาเป็นวินาทีต่อใบ — คนเข้าคนแรกของแต่ละหน้าต้องนั่งรอ
      ตรงนี้ทั้งดุ้น WebP เข้ารหัสเร็วกว่าหลายเท่าและไฟล์ต้นทางก็เป็น WebP อยู่แล้ว
    */
    formats: ["image/webp"],
    /*
      Next 16 บังคับให้ประกาศค่า quality ที่อนุญาตไว้ล่วงหน้า
      และค่าที่ไม่อยู่ในลิสต์จะถูกปัดไปหาค่าใกล้สุด ไม่ใช่ error
      จึงเหลือไว้เท่าที่ใช้จริง กันเผลอส่งเลขแปลก ๆ แล้วโดนปัดขึ้นเงียบ ๆ
    */
    qualities: [75, 85, 100],
    /*
      คงเพดาน 2560 ไว้เท่าความกว้างไฟล์ต้นทาง

      เคยลองตัดเหลือ 1920 เพื่อลดงานย่อภาพ แต่แบนเนอร์เต็มจอบนจอ Retina
      ต้องใช้พิกเซลราวสองเท่าของความกว้างจอ พอมีให้แค่ 1920 ภาพจึงโดนขยายจนเบลอ
      เห็นชัดมาก ความเร็วที่ได้ไม่คุ้มกับความคมที่เสีย
      ส่วน 3840 ยังตัดทิ้งเหมือนเดิม เพราะย่อภาพ 4K กินแรมจนคอนเทนเนอร์ตาย (exit 137)
    */
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 2560],
    // แคชไฟล์ที่ optimize แล้ว 30 วัน ลดงานฝั่งเซิร์ฟเวอร์ตอนคนเข้าซ้ำ
    minimumCacheTTL: 2592000,
  },
};

export default nextConfig;
