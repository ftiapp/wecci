/**
 * แถบสีรองข้อความบนแบนเนอร์
 *
 * แทนที่จะคลุมเงาทับทั้งภาพจนสีเพี้ยน จะรองสีเฉพาะบริเวณที่มีตัวอักษร
 * แล้วจางหายไปทางขวา ภาพส่วนที่เหลือจึงคงสีจริงไว้ทั้งหมด
 * แถบบานทะลุออกไปชนขอบจอซ้ายเสมอ ไม่ว่า Container จะกว้างเท่าไร
 */
export function HeroPanel({
  children,
  className = "",
}: {
  children: React.ReactNode;
  className?: string;
}) {
  return (
    <div className={`relative ${className}`}>
      {/*
        ไม่มีแผ่นสีรองหลังข้อความแล้ว ภาพจึงไม่ถูกบังหรือย้อมสี
        มือถือใช้เงาใต้ตัวอักษรแทน — กันตัวขาวจมไปกับส่วนสว่างของภาพ
        โดยไม่ทำให้เห็นเป็นก้อนสีทึบ ๆ พาดอยู่บนแบนเนอร์
      */}
      <div className="relative [text-shadow:0_1px_10px_rgba(0,0,0,0.55)] sm:[text-shadow:none]">
        {children}
      </div>
    </div>
  );
}
